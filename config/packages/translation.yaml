framework:
    default_locale: '%locale%'
    translator:
        paths:
            - '%kernel.project_dir%/config/translations'
            - '%kernel.project_dir%/translations'
        fallbacks:
            - '%locale%'
