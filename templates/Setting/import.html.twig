{% trans_default_domain 'System' %}

{% form_theme form
    '@HillrangeForm/FormTheme/bootstrap.html.twig'
%}

{{ form_start(form, {attr: {class: 'form-inline'}}) }}

<div class="row">
    <div class="col-auto">
        {{ form_widget(form.import_file) }}
    </div>
    <div class="col-auto">
        {{ saveButton({class: 'btn btn-info fas fa-cloud-upload-alt', style: ''})|raw }}
    </div>
</div>

{{ form_end(form) }}