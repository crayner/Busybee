{% trans_default_domain "Staff" %}

{% include "Person/panelHeader.html.twig"  with {header: 'person.staff.title', name: personManager.person.formatName} %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-6 card">
                {{ form_row(form.staffType) }}
            </div>
            <div class="col-6 card">
                {{ form_row(form.jobTitle) }}
            </div>
        </div>
        <div class="row">
            <div class="col-6 card">
                {{ form_row(form.house) }}
            </div>
            <div class="col-6 card">
                {#{{ form_row(form.staff.homeroom) }}#}
            </div>
        </div>
    </div>
{% include 'Page/panelEnd.html.twig' %}