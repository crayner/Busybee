{% if pagination.choice is defined %}
    <script type="text/javascript" language="JavaScript">
        $('.paginatorChoice').change(function () {
            $('#{{ pagination.idName }}').prop('action', $(this).val());
            $('#{{ pagination.idName }}').submit();
        });
    </script>
    <!-- pagination Choice -->
{% endif %}

