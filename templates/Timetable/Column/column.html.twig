{% trans_default_domain "Timetable" %}

{% set active = ' active' %}
{% for name,col in form.columns %}
    <div class="tab-pane{{ active }}" id="{{ col.code.vars.value }}">
        {% include 'Timetable/Column/columnContent.html.twig' %}
    </div>
    {% do col.setRendered %}
    {% set active = '' %}
{% endfor %}

